"Zero-chex: A compliance-focused implementation of chex.\n\nThis module provides type aliases, assertions, and utilities matching the\n`chex` public API.\n"

from ._src.asserts.scalar import (
    assert_scalar,
    assert_scalar_in,
    assert_scalar_negative,
    assert_scalar_non_negative,
    assert_scalar_positive,
)
from ._src.asserts.basic import (
    assert_devices_available,
    assert_equal,
    assert_exactly_one_is_none,
    assert_gpu_available,
    assert_is_broadcastable,
    assert_is_divisible,
    assert_not_both_none,
    assert_tpu_available,
)
from ._src.asserts.tensors import (
    assert_axis_dimension,
    assert_axis_dimension_comparator,
    assert_axis_dimension_gt,
    assert_axis_dimension_gteq,
    assert_axis_dimension_lt,
    assert_axis_dimension_lteq,
    assert_equal_rank,
    assert_equal_shape,
    assert_equal_shape_prefix,
    assert_equal_shape_suffix,
    assert_equal_size,
    assert_rank,
    assert_shape,
    assert_size,
    assert_type,
)
from ._src.asserts.trees import (
    assert_tree_all_finite,
    assert_tree_has_only_ndarrays,
    assert_tree_is_on_device,
    assert_tree_is_on_host,
    assert_tree_is_sharded,
    assert_tree_no_nones,
    assert_tree_shape_prefix,
    assert_tree_shape_suffix,
    assert_trees_all_close,
    assert_trees_all_close_ulp,
    assert_trees_all_equal,
    assert_trees_all_equal_comparator,
    assert_trees_all_equal_dtypes,
    assert_trees_all_equal_shapes,
    assert_trees_all_equal_shapes_and_dtypes,
    assert_trees_all_equal_sizes,
    assert_trees_all_equal_structs,
)
from ._src.misc import (
    assert_max_traces,
    assert_numerical_grads,
    block_until_chexify_assertions_complete,
    chexify,
    clear_trace_counter,
    create_deprecated_function_alias,
    dataclass,
    disable_asserts,
    enable_asserts,
    fake_jit,
    fake_pmap,
    fake_pmap_and_jit,
    if_args_not_none,
    mappable_dataclass,
    params_product,
    restrict_backends,
    set_n_cpu_devices,
    warn_deprecated_function,
    warn_only_n_pos_args_in_future,
    with_jittable_assertions,
)
from ._src.classes import ChexVariantType, Dimensions, TestCase
from ._src.types import (
    ArrayBatched,
    ArrayDevice,
    ArrayNumpy,
    ArraySharded,
    Device,
    PRNGKey,
    PyTreeDef,
)

__all__ = [
    "ArrayBatched",
    "ArrayDevice",
    "ArrayNumpy",
    "ArraySharded",
    "ChexVariantType",
    "Device",
    "Dimensions",
    "PRNGKey",
    "PyTreeDef",
    "TestCase",
    "assert_axis_dimension",
    "assert_axis_dimension_comparator",
    "assert_axis_dimension_gt",
    "assert_axis_dimension_gteq",
    "assert_axis_dimension_lt",
    "assert_axis_dimension_lteq",
    "assert_equal_rank",
    "assert_equal_shape",
    "assert_equal_shape_prefix",
    "assert_equal_shape_suffix",
    "assert_equal_size",
    "assert_rank",
    "assert_shape",
    "assert_size",
    "assert_tree_all_finite",
    "assert_tree_has_only_ndarrays",
    "assert_tree_is_on_device",
    "assert_tree_is_on_host",
    "assert_tree_is_sharded",
    "assert_tree_no_nones",
    "assert_tree_shape_prefix",
    "assert_tree_shape_suffix",
    "assert_trees_all_close",
    "assert_trees_all_close_ulp",
    "assert_trees_all_equal",
    "assert_trees_all_equal_comparator",
    "assert_trees_all_equal_dtypes",
    "assert_trees_all_equal_shapes",
    "assert_trees_all_equal_shapes_and_dtypes",
    "assert_trees_all_equal_sizes",
    "assert_trees_all_equal_structs",
    "assert_type",
    "assert_devices_available",
    "assert_equal",
    "assert_exactly_one_is_none",
    "assert_gpu_available",
    "assert_is_broadcastable",
    "assert_is_divisible",
    "assert_not_both_none",
    "assert_tpu_available",
    "assert_max_traces",
    "assert_numerical_grads",
    "block_until_chexify_assertions_complete",
    "chexify",
    "clear_trace_counter",
    "create_deprecated_function_alias",
    "dataclass",
    "disable_asserts",
    "enable_asserts",
    "fake_jit",
    "fake_pmap",
    "fake_pmap_and_jit",
    "if_args_not_none",
    "mappable_dataclass",
    "params_product",
    "restrict_backends",
    "set_n_cpu_devices",
    "warn_deprecated_function",
    "warn_only_n_pos_args_in_future",
    "with_jittable_assertions",
    "assert_scalar",
    "assert_scalar_in",
    "assert_scalar_negative",
    "assert_scalar_non_negative",
    "assert_scalar_positive",
]
